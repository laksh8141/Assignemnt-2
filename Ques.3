def duplicate(lst):
    duplicates = []
    seen = set()
    
    for num in lst:
        if num in seen:
            if num not in duplicates:
                duplicates.append(num)
        else:
            seen.add(num)
    
    return duplicates

numbers = [1, 2, 3, 2, 4, 5, 6, 7, 8, 9, 1]
print("Duplicates:", duplicate(numbers))
